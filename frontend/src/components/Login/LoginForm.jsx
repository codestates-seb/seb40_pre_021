import styled from 'styled-components';

const LoginFormStyle = styled.div`
	border-radius: 3px;

	background-color: white;
`;
const LoginForm = () => {
	return <LoginFormStyle>login</LoginFormStyle>;
};

export default LoginForm;
